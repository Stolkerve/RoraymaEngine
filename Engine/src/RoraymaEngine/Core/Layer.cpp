#include "Layer.hh"

/* All its code is in base (copy and paste) of layers system on Hazel Engine developed by The Cherno. */

namespace rym
{
	Layer::Layer(const std::string_view& name) :
		m_Name(name)
	{
	}
}